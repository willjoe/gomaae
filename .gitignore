node_modules/
.env
.DS_Store
coverage/
dist/
build/
.next/
out/
DocsAssets/

# Claude Code local session state (settings, worktrees)
.claude/

# Legacy pre-flatten directory (runtime DBs, logs, stale source copy)
gomaae/

# Generated incremental TypeScript build cache
tsconfig.tsbuildinfo

# Local machine config (absolute workspace paths + UI prefs)
config.yaml

*storybook.log
storybook-static

# Playwright end-to-end tests, recordings, and run artifacts
e2e/
test-results/
playwright-report/

# Runtime databases and logs
*.db
server.log
